-r requirements.txt

# Testing
pytest>=8.1.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
moto[kinesis,dynamodb,sagemaker]>=5.0.0

# Local AWS emulation
localstack>=3.3.0

# Code quality
ruff>=0.4.0
bandit>=1.7.8
mypy>=1.9.0
boto3-stubs[kinesis,dynamodb,sagemaker-runtime,iot]>=1.34.0

# Signal processing (also used in training)
torch>=2.2.0
torchvision>=0.17.0
scikit-learn>=1.4.0
pandas>=2.2.0
pyarrow>=15.0.0

# SageMaker SDK (pipeline authoring, model registry)
sagemaker>=2.214.0
