-r requirements.txt

# Testing
pytest>=8.3.0
pytest-cov>=6.0.0
pytest-mock>=3.14.0
moto[kinesis,dynamodb,sagemaker]>=5.0.0

# Local AWS emulation
localstack>=3.8.0

# Code quality
ruff>=0.6.0
bandit>=1.8.0
mypy>=1.11.0
boto3-stubs[kinesis,dynamodb,sagemaker-runtime,iot]>=1.35.0

# Signal processing (also used in training)
torch>=2.4.0
torchvision>=0.19.0
scikit-learn>=1.5.0
pandas>=2.2.0
pyarrow>=17.0.0

# SageMaker SDK (pipeline authoring, model registry)
sagemaker>=2.232.0
